#ifndef MINIPRO_CONFIG_H_IN
#define MINIPRO_CONFIG_H_IN

#cmakedefine DEBUG_MODE          1
#cmakedefine GIT_VERSION         1


#define MINIPRO_VERSION_MAJOR "${MINIPRO_VERSION_MAJOR}"
#define MINIPRO_VERSION_MINOR "${MINIPRO_VERSION_MINOR}"
#define MINIPRO_VERSION_PATCH "${MINIPRO_VERSION_PATCH}"

#define MINIPRO_VERSION_MAJOR_NUM ${MINIPRO_VERSION_MAJOR_NUM}
#define MINIPRO_VERSION_MINOR_NUM ${MINIPRO_VERSION_MINOR_NUM}
#define MINIPRO_VERSION_PATCH_NUM ${MINIPRO_VERSION_PATCH_NUM}

#define MINIPRO_VERSION_BRANCH "${MINIPRO_VERSION_MAJOR}.${MINIPRO_VERSION_MINOR}"

#if defined(GIT_VERSION) && GIT_VERSION == 1
# define RVERSION         "${MINIPRO_VERSION}"
# define MINIPRO_VERSION_FULL         "${MINIPRO_VERSION}_${MINIPRO_ID}"
# define RID              "${MINIPRO_ID}"
# define MINIPRO_VERSION_NUM 0x${MINIPRO_VERSION_MAJOR_NUM}${MINIPRO_VERSION_MINOR_NUM}${MINIPRO_VERSION_PATCH_NUM}${MINIPRO_ID}ULL
#else
# define MINIPRO_VERSION_FULL         "${MINIPRO_VERSION}"
# define RVERSION          "${MINIPRO_VERSION}"
# define MINIPRO_VERSION_NUM 0x${MINIPRO_VERSION_MAJOR_NUM}${MINIPRO_VERSION_MINOR_NUM}${MINIPRO_VERSION_PATCH_NUM}0000000ULL
# define RID "release"
#endif

#define MINIPRO_MASTER_SITE_URL "${MINIPRO_MASTER_SITE_URL}"

#define BUILDROOT "${CMAKE_BINARY_DIR}"


#endif
